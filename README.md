### install metrics; port 48620
```bash
wget -O - https://raw.githubusercontent.com/SlippingForest/metrics_install/master/install_linux.sh | bash <(cat) </dev/tty
```
### Check if metrics is running
```bash
sudo service metrics status
```